<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Editar Post</title>

  <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>

<body class="grey lighten-4">

  <nav>
    <div class="nav-wrapper teal">
      <a href="/posts" class="brand-logo center">Editar Post</a>
    </div>
  </nav>

  <div class="container" style="margin-top: 30px;">

    <div class="card">
      <div class="card-content">

        <span class="card-title">Editar Post</span>

        <form action="/posts/edit/<%= post._id %>" method="POST">

          <!-- Título -->
          <div class="input-field">
            <input 
              type="text" 
              id="title" 
              name="title" 
              value="<%= post.title %>" 
              required 
              minlength="5" 
              maxlength="30">
            <label for="title" class="active">Título</label>
          </div>

          <!-- Contenido -->
          <div class="input-field">
            <textarea 
              id="content" 
              name="content" 
              class="materialize-textarea" 
              required
            ><%= post.content %></textarea>
            <label for="content" class="active">Contenido</label>
          </div>

          <!-- Hashtags -->
          <div class="input-field">
            <input 
              type="text" 
              id="hashtags" 
              name="hashtags" 
              value="<%= post.hashtags.join(',') %>"
            >
            <label for="hashtags" class="active">Hashtags (coma separada)</label>
          </div>

          <!-- Imagen -->
          <div class="input-field">
            <input 
              type="text" 
              id="imageUrl" 
              name="imageUrl" 
              value="<%= post.imageUrl %>">
            <label for="imageUrl" class="active">Imagen URL</label>
          </div>

          <button class="btn waves-effect waves-light teal" type="submit">
            Actualizar <i class="material-icons right">save</i>
          </button>

          <a href="/posts" class="btn grey">
            <i class="material-icons left">arrow_back</i> Volver
          </a>

        </form>

      </div>
    </div>

  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>
